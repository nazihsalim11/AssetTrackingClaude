# Base URL of the deployed backend's API, including the /api prefix
# (e.g. the Railway service URL). Leave unset for local dev — Vite's dev
# server proxy (vite.config.ts) forwards /api and /uploads to localhost:4000.
VITE_API_URL=https://your-railway-app.up.railway.app/api
